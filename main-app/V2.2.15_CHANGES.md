# v2.2.15（2026年9月8日）

対象：昌吾用（main-app）

- 予定タブ上部に「本日の予定」を追加。月選択とは独立して当日の記録を表示。
- 場所・現場名・作業内容・作業者・住所・座標・メモを現場別に表示。
- Googleマップと既存資料の閲覧に対応。
- 下段は選択月の明日以降の予定を日付順で表示。
- 日付変更と画面復帰に対応。日付・月・タブ変更時に削除選択を解除。
- 既存の勤務記録表示とテーマを維持。

検証：ビルドと成果物検証成功。既存テスト1件成功。
当日／未来の順序、選択月に依存しない当日表示、地図・資料の操作表示、空状態、休み、複数現場の表示確認成功。
型チェックは更新前と同じ既存エラーのみで、追加エラーなし。
対話プレビューは既存Worker互換日付とランタイムの対応日付の不一致で起動不可。設定は変更していない。
実際の資料取得とスマホ実画面、本番動作は未確認。
DB・API・OAuth・カレンダー同期・清田さん用は変更なし。Drive/Sheets移行は保留。
commit・push・デプロイは未実施。
